Adds /results/manage page with a visible Delete button to remove readings.
Copy files into your repo. Run the SQL once in Supabase. Visit /results/manage.
Git:
  git add -A
  git commit -m "feat(results): add manage page with delete"
  git push
